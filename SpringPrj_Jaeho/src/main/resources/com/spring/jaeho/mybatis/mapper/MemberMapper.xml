<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    <mapper namespace="com.spring.jaeho.mybatis.mapper.MemberMapper">
    <!-- 
    <insert id="insertBoard" parameterType="MemberDTO">
		INSERT INTO member
		(m_id,m_name,m_zip1,m_zip2,m_zip3,m_password,m_userEmail)
		VALUES(#{b_title},#{b_writer},#{b_detail},SYSDATE())
	</insert> -->
    
    </mapper>